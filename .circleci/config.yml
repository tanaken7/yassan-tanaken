version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout

      - run:
          name: Install npm
          command: npm install


workflows:
  version: 2
  main:
    jobs:
      - build
